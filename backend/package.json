{
  "name": "@gift-safe/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "build": "node -e \"console.log('Backend build step is not required for this package')\"",
    "lint": "node --check src/server.js && node --check src/app.js && node --check src/services/spinService.js && node --check src/services/externalDiscounts.js && node --check src/services/insalesApi.js && node --check src/services/notifier.js && node --check src/services/promoPool.js && node --check src/services/expirer.js && node --check src/middleware/rateLimit.js && node --check prisma/seed.js",
    "test": "node --test",
    "smoke": "node scripts/smoke.mjs",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push && prisma generate",
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.6.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "node-cron": "^4.2.1",
    "nodemailer": "^7.0.10",
    "pg": "^8.16.3",
    "pino": "^10.1.0",
    "pino-pretty": "^13.1.2",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "prisma": "^7.6.0"
  }
}
